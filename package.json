{
  "scripts": {
    "prebuild": "rimraf ./dist",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node ./dist/index.js"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.3.2"
  },
  "dependencies": {
    "dotenv": "^10.0.0",
    "puppeteer": "^10.0.0"
  }
}
